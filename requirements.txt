# TexITex — Token-Image Diffusion
# Python 3.9 (system: /usr/bin/python3 on macOS)
# Tested on Apple M4 Mac Mini, 64GB, macOS Sequoia

torch==2.3.1          # MPS backend (Apple Silicon)
transformers>=4.37.0  # Qwen/Qwen2.5-1.5B tokenizer + model
scikit-learn>=1.3.0   # PCA (legacy encoder)
numpy>=1.24.0
tqdm>=4.66.0
matplotlib>=3.8.0
Pillow>=10.0.0
